name: CI

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Checkout mcporch sibling for file:../mcporch dependency
      - uses: actions/checkout@v4
        with:
          repository: superbenefit/mcporch
          path: ../mcporch

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run type-check
      - run: npm test
